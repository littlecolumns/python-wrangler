<template>
  <div>
    <p>To install pipenv on OS X, you'll need to first install <a href="https://brew.sh/">Homebrew</a>. Once that's done you can run <code>brew install pipenv</code>.</p>
    <p>If you're using Windows, first install any version of Python some other way and then use <code>pip</code> to install pipenv:</p>
    <pre><code>pip install pipenv</code></pre>
    <p>You can find more details <a href="https://docs.pipenv.org/">in the documentation</a>.</p>
  </div>
</template>

<script>
export default {

}
</script>

<style>
</style>
